{% extends 'base.html.twig' %}

{% block title %}Administrator{% endblock %}

{% block body %}
    <h1>Administrator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ administrator.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ administrator.name }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ administrator.lastName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ administrator.email }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ administrator.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ administrator.mdp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administrator_index') }}">back to list</a>

    <a href="{{ path('app_administrator_edit', {'id': administrator.id}) }}">edit</a>

    {{ include('administrator/_delete_form.html.twig') }}
{% endblock %}
